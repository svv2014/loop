# User-specific config (contains local paths, secrets references)
loop.env
config/projects.yaml

# Runtime artifacts
/tmp/
*.lock
/data/bounties.jsonl

# Local tooling state
.claude/

# Operator-specific workflow overrides — local only. Each operator authors
# their own legacy/internal workflows; `default.yaml`, `minimal.yaml`, and
# `docs-only.yaml` ship in the public repo.
config/workflows/current.yaml
config/workflows/*.local.yaml
